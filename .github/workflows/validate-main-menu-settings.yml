name: Validate main menu settings

on:
  pull_request:
    paths:
      - "scenes/menus/main_menu.tscn"
      - "scenes/ui/main_menu_settings_panel.tscn"
      - "scripts/menus/main_menu.gd"
      - "scripts/menus/main_menu_atmosphere.gd"
      - "scripts/menus/main_menu_title_glow.gd"
      - "scripts/settings/interface_settings_store.gd"
      - "scripts/ui/main_menu_settings_panel.gd"
      - "tests/test_main_menu_settings.py"
      - "tests/smoke_main_menu_settings.gd"
      - "docs/MAIN_MENU_SETTINGS_V01.md"
      - ".github/workflows/validate-main-menu-settings.yml"
  push:
    branches:
      - feat/main-menu-settings-v01

concurrency:
  group: main-menu-settings-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate static contracts
        run: python tests/test_main_menu_settings.py

      - name: Download Godot 4.7.1 Standard
        shell: bash
        run: |
          curl --location --fail --retry 3 \
            --output godot.zip \
            "https://github.com/godotengine/godot/releases/download/4.7.1-stable/Godot_v4.7.1-stable_linux.x86_64.zip"
          unzip -q godot.zip
          chmod +x Godot_v4.7.1-stable_linux.x86_64

      - name: Import project and reject parser errors
        shell: bash
        run: |
          rm -rf .godot
          set -o pipefail
          timeout 180s ./Godot_v4.7.1-stable_linux.x86_64 \
            --headless --editor --path "$GITHUB_WORKSPACE" --quit \
            2>&1 | tee main-menu-settings-import.log
          if grep -E "SCRIPT ERROR|Parse Error|Failed to load script" main-menu-settings-import.log; then
            exit 1
          fi

      - name: Smoke test settings runtime
        shell: bash
        run: |
          timeout 120s ./Godot_v4.7.1-stable_linux.x86_64 \
            --headless --path "$GITHUB_WORKSPACE" \
            --script res://tests/smoke_main_menu_settings.gd \
            2>&1 | tee main-menu-settings-smoke.log
          grep -F "Main menu settings smoke test passed" main-menu-settings-smoke.log

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: main-menu-settings-diagnostics
          path: |
            main-menu-settings-import.log
            main-menu-settings-smoke.log
          if-no-files-found: ignore
          retention-days: 7
