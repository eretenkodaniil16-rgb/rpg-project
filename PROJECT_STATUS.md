# PROJECT STATUS

Дата обновления: 2026-07-29

## Текущая интеграционная точка

- Движок: Godot 4.7.1 Standard, без .NET.
- Основная платформа распространения: Android.
- Тестовая платформа: Windows.
- Механическая цепочка PR №47–55 последовательно объединена в `main`.
- PR №56 завершает интеграционную стабилизацию v0.18.
- Арт-PR №42 остаётся отдельным draft и не входит в механическую интеграцию.
- Техническая версия приложения в `project.godot` пока остаётся `0.17.0`; название v0.18 относится к интеграционному этапу, а не к опубликованному релизу.

## Выполненный порядок слияния

```text
#47 feature/level-up-progression-v2
→ #48 feature/experience-reward-pipeline
→ #49 feature/universal-level-choices
→ #50 feature/fighter-subclass-mechanics
→ #51 feature/unified-encounter-system
→ #52 feature/hidden-combat-escape
→ #53 feature/exploration-stealth-alerts
→ #54 feature/npc-patrol-alert-groups
→ #55 feature/npc-context-actions-ai-foundation
→ #56 integration/v018-stabilization
```

Порядок соблюдён. Перед каждым слиянием следующий PR был переназначен на фактический `main`, после чего проверялись mergeability и профильные workflow.

## Merge-коммиты этапов

- PR №47: `9c1a0c4991021db9d27d45ff7cff14e814163c9a`;
- PR №48: `ca1d9ba3cf49cb24848fb905c890a61f682bd7a4`;
- PR №49: `fc03c646812a28db21390011dfc70712efe0481c`;
- PR №50: `14c10d78c231626d15aefb3949958eea3f432a31`;
- PR №51: `3b0cd4a0e09e155c7c516afdc82af0e2a59f9f66`;
- PR №52: `58e6b356420dd97fdcda0ffb244869f99500abd6`;
- PR №53: `c3ec7b346f8774be9997337e0a9552278c388d95`;
- PR №54: `10131d4685827baefbdba5f4cc7b528d1433a964`;
- PR №55: `404104aa01ed1b835942b08f54025ca41c9aba3b`.

Финальный merge-коммит PR №56 фиксируется в истории `main` после объединения этого PR.

## Интегрированные системы

- транзакционное повышение уровня и уровневые выборы;
- data-driven XP-награды без повторной выдачи;
- исполняемые подклассы Воина;
- единый encounter lifecycle с `resolved`, `failed` и `abandoned`;
- сложное бегство через укрытия и потерю следа;
- скрытность, шум и тревога вне боя;
- двери, комнаты и укромные места со стабильными ID;
- патрули и локальные alert-группы;
- `NavigationAgent2D` для патруля и расследования;
- контролируемое присоединение подкрепления к инициативе;
- основа data-driven AI NPC;
- контекстный осмотр NPC без раскрытия точных HP, КД и подозрения;
- единая нижняя кнопка `ДЕЙСТВИЯ` и отдельная кнопка `ЦЕЛЬ`.

## Интеграционный контракт v0.18

Автоматическая проверка `tests/test_v018_integration_stack.gd` подтверждает:

- autoload `GameState` указывает на `game_state_stealth_alerts.gd`;
- итоговый `GameState` содержит API наград, encounter и stealth-alert;
- XP-награда, `abandoned` encounter, тревога NPC и состояние двери записываются в один save-файл;
- реестры `_claimed_experience_rewards_v1`, `encounter_registry_v1` и `stealth_alert_registry_v1` переживают загрузку;
- игровая сцена использует `game_pursuit_escape_runtime.gd`;
- окно повышения уровня использует допустимый `z_index = 4095`;
- в HUD остаётся одна нижняя кнопка `ДЕЙСТВИЯ` и отдельная кнопка `ЦЕЛЬ`.

Профильный workflow: `.github/workflows/validate-v018-integration.yml`.

## Проверки перед финальным слиянием

На head PR №56 успешно прошли:

- `Validate v0.18 integration stack`;
- `Validate Godot project`;
- `Validate v0.17 UI`;
- `Validate level-up progression`;
- `Validate character creator`;
- `Validate racial traits`;
- обычная Android APK;
- Mobile Test APK.

## Закрытый технический долг

- `LevelUpPanel` больше не устанавливает недопустимый `z_index = 5000`.
- Новое значение `4095` находится в допустимом диапазоне Godot.

## Ручная проверка на Android

До объявления v0.18 проверенной пользователь должен подтвердить на физическом устройстве:

- кнопка `ЦЕЛЬ` остаётся в исследовании и бою;
- нижняя кнопка `ДЕЙСТВИЯ` открывает меню одним нажатием;
- кнопки не перекрывают маршрут, `АТАКУ` и `КОНЕЦ ХОДА`;
- дозорный проходит только через открытую дверь;
- подкрепление один раз присоединяется к инициативе;
- скрытность, поиск и сложное бегство читаются без точных процентов;
- сохранение и загрузка сохраняют XP, encounter, тревогу и дверь.

## Не завершено

- физический Android smoke-test интеграционной сборки;
- Combat AI v1 для ролей ближнего бойца, стрелка и защитника;
- утверждение и импорт игровых кадров из арт-PR №42;
- официальный релизный bump версии приложения до `0.18.0`.

## Следующий точный шаг

После проверки интеграционной APK на физическом Android-устройстве исправить найденные мобильные дефекты отдельным PR. При отсутствии блокирующих дефектов следующим механическим этапом становится Combat AI v1.